# 放到平台所在服务器的:
#   - 全局: /etc/npmrc  或  $(npm config get globalconfig)
#   - 或当前用户: ~/.npmrc
#
# 把 NEXUS_HOST 换成离线服务器的内网 IP 或主机名。

registry=http://NEXUS_HOST:7012/repository/npm-hosted/
# 允许非 https 源
strict-ssl=false
